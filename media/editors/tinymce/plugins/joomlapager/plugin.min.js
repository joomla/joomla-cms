tinymce.PluginManager.add("joomlapager", function(editor, url) {
	editor.addButton("joomlapager", {
		text: "J Page Break",
		title: "Joomla Page Break",
		icon: "copy",
		onclick: function () {
			editor.windowManager.open({
				title   : 'Page Break',
				body    : [{
					type : 'textbox',
					name : 'title',
					label: 'Title:'
				},
					{
						type : 'textbox',
						name : 'id',
						label: 'Alternative:'
					}],
				onsubmit: function (e) {
					editor.insertContent('<hr class="system-pagebreak" title="' + e.data.title + '" alt="' + e.data.id + '" />');
				}
			});
		}
	}),
		editor.addMenuItem("joomlapager", {
		text: "J Page Break",
		title: "Joomla Page Break",
		icon: "copy",
		onclick: function () {
			editor.windowManager.open({
				title   : 'Page Break',
				body    : [{
					type : 'textbox',
					name : 'title',
					label: 'Title:'
				},
					{
						type : 'textbox',
						name : 'id',
						label: 'Alternative:'
					}],
				onsubmit: function (e) {
					editor.insertContent('<hr class="system-pagebreak" title="' + e.data.title + '" alt="' + e.data.id + '" />');
				}
			});
		}
	})
});