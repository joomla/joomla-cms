tinymce.PluginManager.add("joomlamore", function(a) {
	a.addCommand("mceJoomlaMore", function() {
		var myRe = /<hr\s*(id="system-readmore")/gi,
			tiny_text = a.getContent(),
			is_set = myRe.exec(tiny_text);

		if (is_set != null) {
			alert(hrExists);
		}
		else
		{
			a.insertContent("<hr id=\"system-readmore\" />");
		}
	}), a.addButton("joomlamore", {
		text: 'J Read More',
		title: "Joomla Read More",
		icon: 'pagebreak',
		cmd: "mceJoomlaMore"
	}), a.addMenuItem("joomlamore", {
		text: "Joomla Read More",
		icon: "pagebreak",
		cmd: "mceJoomlaMore",
		context: "insert"
	})
});
