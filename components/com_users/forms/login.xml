<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset name="credentials_placeholders" label="COM_USERS_LOGIN_DEFAULT_LABEL">
		<field
			name="username"
			type="text"
			class="validate-username"
			filter="username"
			size="25"
			validate="username"
			autofocus="true"
			autocomplete="username"
			hint="COM_USERS_LOGIN_USERNAME_LABEL"
		/>

		<field
			name="password"
			type="password"
			filter="raw"
			size="25"
			autocomplete="current-password"
			hint="JGLOBAL_PASSWORD"
		/>
	</fieldset>
	<fieldset name="credentials_text" label="COM_USERS_LOGIN_DEFAULT_LABEL">
		<field
			name="username"
			type="text"
			label="COM_USERS_LOGIN_USERNAME_LABEL"
			class="validate-username"
			filter="username"
			size="25"
			required="true"
			validate="username"
			autofocus="true"
			autocomplete="username"
		/>

		<field
			name="password"
			type="password"
			label="JGLOBAL_PASSWORD"
			required="true"
			filter="raw"
			size="25"
			autocomplete="current-password"
		/>
	</fieldset>

	<fieldset>
		<field
			name="return"
			type="hidden"
		/>
	</fieldset>
</form>
